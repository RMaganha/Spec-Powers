# Plano — bpmn (desenho do processo a partir do código)

Spec viva: `docs/specs/bpmn.md`. Nível de cerimônia: **médio**. Uma task por vez, TDD, saída colada.

- **T1 — extração (`templates/bpmn.py`)**: portas de entrada (rota Flask/FastAPI · `main()`/`if __name__`
  na raiz quando não há rota) → árvore de nós por `ast`. Cobre AC1, AC2, AC3, AC4, AC5, AC6, AC8.
- **T2 — `render_texto` → `docs/bpmn.md`**: processos, passos ordenados, tipo de elemento, raia, desfechos,
  seção "não lido", legenda do que não é derivável. Cobre a metade "documentação" do AC9.
- **T3 — `render_html` → `docs/bpmn.html`**: SVG com raias/piscinas, seletor de processo, pan/zoom,
  self-contained (zero CDN). Cobre AC7 (rastro `… (+N)`), AC9, AC10 (`node --check`).
- **T4 — comando + wiring**: `commands/bpmn.md`, âncora `/docs/bpmn.*` nos **dois** `.gitignore`,
  linha no `docs/LEIA-ME.md`, `test_bpmn_wiring` no smoke.
- **T5 — dogfood + release**: rodar no próprio repo e colar a saída; 0.24.0 nos dois manifestos; CHANGELOG.
