<!-- MODELO — copie para docs/decisoes.md. Log de DECISÕES TRANSVERSAIS do projeto (arquitetura,
     escolha de lib, padrão) — NÃO as de um assunto só (essas ficam no "Histórico" da spec em
     docs/specs/<assunto>.md). 1 linha por decisão, mantido pelo /mss-spec:nova-feature (passo de
     fecho) e por qualquer decisão significativa. Formato:
       - <data> — decidimos <X> em vez de <Y> — porque <Z>. -->

# Decisões — <Projeto>

_(ainda sem decisões registradas; a primeira decisão transversal do projeto entra aqui)_
